# name: workflow-template-check.yaml
# version: 0.4.8
# Description: Check if GitHub Actions workflows match gsm.utils templates
name: Workflow Template Compliance Check
on:
  pull_request:
    branches: [main, release, dev]
    types: [opened, synchronize, reopened]
  schedule:
    - cron: '0 11 * * *'
  repository_dispatch:
    types: [workflow-templates-updated]
  workflow_dispatch:
permissions:
  pull-requests: write
  contents: write
jobs:
  check-workflows:
    uses: Gilead-BioStats/gsm.utils/.github/workflows/workflow-template-checker.yaml@fix-117-auto-update-workflows
    with:
      default-branch: ${{ github.event.repository.default_branch }}
      pr-number: '${{ github.event.number }}'
      event-name: ${{ github.event_name }}
    secrets:
      app-id: ${{ secrets.CM_BOT_APP_ID }}
      app-private-key: ${{ secrets.CM_BOT_PRIVATE_KEY }}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/QCMergeGH.R
\name{BatchLookupCommitParents}
\alias{BatchLookupCommitParents}
\title{Look up parent SHAs for multiple commits using git2r}
\usage{
BatchLookupCommitParents(chrSHAs, strPkgRoot = ".")
}
\arguments{
\item{chrSHAs}{(\code{character}) A vector of commit SHAs.}

\item{strPkgRoot}{(\verb{length-1 character}) The path to a directory in the
package. Will be expanded using \code{\link[gert:git_repo]{gert::git_find()}}.}
}
\value{
A list of character vectors, one per element of \code{chrSHAs}, each
containing the parent SHAs for that commit, or \code{character(0)} if the
commit is not available locally.
}
\description{
Uses \code{\link[git2r:lookup]{git2r::lookup()}} and \code{\link[git2r:parents]{git2r::parents()}} to efficiently retrieve parent
SHAs for many commits in a single repository session. This is much faster
than calling \code{\link[gert:git_commit]{gert::git_commit_info()}} per commit for large repositories.
}
\details{
If a SHA is not found locally (e.g., a squash-merge commit that GitHub
records but never existed in the local history), \code{character(0)} is returned
for that element.
}
\keyword{internal}

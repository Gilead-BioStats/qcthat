% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/QCMergeGH.R
\name{FetchAllMergeCommitSHAsLocal}
\alias{FetchAllMergeCommitSHAsLocal}
\title{Fetch commit SHAs for multiple merged PRs using a single local git log}
\usage{
FetchAllMergeCommitSHAsLocal(
  chrMergeSHAs,
  strPkgRoot = ".",
  intMaxCommits = 100000L
)
}
\arguments{
\item{chrMergeSHAs}{(\code{character}) A vector of merge commit SHAs.}

\item{strPkgRoot}{(\verb{length-1 character}) The path to a directory in the
package. Will be expanded using \code{\link[gert:git_repo]{gert::git_find()}}.}

\item{intMaxCommits}{(\verb{length-1 integer}) Maximum number of commits to fetch
from the log. Increase if the repository history exceeds this number.}
}
\value{
A named list, one element per element of \code{chrMergeSHAs}, each a
character vector of commit SHAs introduced by that PR.
}
\description{
Fetches the full repository log once and resolves all PR commit sets in R,
replacing the per-PR calls to \code{\link[=GetGitAheadBehind]{GetGitAheadBehind()}} and \code{\link[=GetGitLog]{GetGitLog()}}. Parent
SHAs are retrieved in batch via \code{\link[=BatchLookupCommitParents]{BatchLookupCommitParents()}} (using \code{git2r}),
which is much faster than per-commit \code{\link[gert:git_commit]{gert::git_commit_info()}} calls in large
repositories.
}
\keyword{internal}

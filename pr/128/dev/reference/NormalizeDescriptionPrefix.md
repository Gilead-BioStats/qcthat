# Normalize the label description prefix

Normalize the label description prefix

## Usage

``` r
NormalizeDescriptionPrefix(strLabelDescription)
```

## Arguments

- strLabelDescription:

  (`length-1 character`) The description for the label.

## Value

The label description with the prefix `"{qcthat}: "`.

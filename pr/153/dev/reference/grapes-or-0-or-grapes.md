# Default value for length-0

Default value for length-0

## Usage

``` r
x %|0|% y
```

## Arguments

- x:

  An object to return if it has length \> 0.

- y:

  An object to return if `x` has length 0.

## Value

`x` if it has length \> 0, otherwise `y`.

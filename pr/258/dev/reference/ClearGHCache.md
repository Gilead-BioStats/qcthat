# Clear the cache used by gh

Clear the cache used by gh

## Usage

``` r
ClearGHCache()
```

## Value

`0` for success, `1` for failure, invisibly (as returned by
[`unlink()`](https://rdrr.io/r/base/unlink.html)).

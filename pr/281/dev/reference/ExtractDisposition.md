# Extract disposition information from a single testthat result

Extract disposition information from a single testthat result

## Usage

``` r
ExtractDisposition(lTestResult)
```

## Arguments

- lTestResult:

  A single element from a `testthat_results` object.

## Value

The string `"fail"`, `"warn"`, `"skip"`, or `"pass"`.

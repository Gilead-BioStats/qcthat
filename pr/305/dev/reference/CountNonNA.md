# Convenience function to count non-NA unique values

Convenience function to count non-NA unique values

## Usage

``` r
CountNonNA(x)
```

## Arguments

- x:

  (`vector`) Vector to count unique non-NA values in.

## Value

An integer representing the number of unique non-NA values in `x`.

# Get the formatted time

Get the formatted time

## Usage

``` r
PrettyTimestamp(strTimezone = "UTC")
```

## Arguments

- strTimezone:

  (`length-1 character`) Timezone to use for formatting.

## Value

A length-1 character vector representing the current time in the given
timezone.

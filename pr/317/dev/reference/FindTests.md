# Find test_that calls in a file

Find test_that calls in a file

## Usage

``` r
FindTests(chrTestLines)
```

## Arguments

- chrTestLines:

  (`character`) Vector of lines from a test file.

## Value

A list of lists, each containing `Test`, `LineStart`, and `LineEnd`.

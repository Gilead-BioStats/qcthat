# Build a GraphQL query to find PRs referencing a specific issue

Build a GraphQL query to find PRs referencing a specific issue

## Usage

``` r
BuildIssuePRRefsQuery(intIssue)
```

## Arguments

- intIssue:

  (`length-1 integer`) The issue with which a check is associated.

## Value

A character string for the GraphQL query.

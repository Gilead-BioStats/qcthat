# Empty issues data frame (raw)

Empty issues data frame (raw)

## Usage

``` r
EmptyIssuesDFRaw()
```

## Value

A standard
[`tibble::tibble()`](https://tibble.tidyverse.org/reference/tibble.html)
with the correct columns (before transformations) but no rows.

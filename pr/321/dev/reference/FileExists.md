# Wrapper for test mocking

Wrapper for test mocking

## Usage

``` r
FileExists(strPath)
```

## Arguments

- strPath:

  (`length-1 character`) Path to check for existence.

## Value

(`logical`) Whether the file exists.

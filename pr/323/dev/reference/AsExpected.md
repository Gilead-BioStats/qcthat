# Assign a class and expected structure to an object

Assign a class and expected structure to an object

## Usage

``` r
AsExpected(x, objShape, chrClass)
```

## Arguments

- x:

  Object to assign the class to.

## Value

An object with the expected structure and class. Note: If `x` has a
length and/or rows, the structure is not validated against `objShape`.

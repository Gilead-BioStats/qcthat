# Finalize a qcthat tree

Finalize a qcthat tree

## Usage

``` r
FinalizeTree(lTree)
```

## Arguments

- lTree:

  (`list` of `character`) A list of character vectors representing parts
  of the qcthat report.

## Value

A character vector representing the finalized tree structure.

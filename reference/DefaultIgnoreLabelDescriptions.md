# Default descriptions for ignored labels

Returns the character vector of descriptions corresponding to the labels
returned by
[`DefaultIgnoreLabels()`](https://gilead-biostats.github.io/qcthat/reference/DefaultIgnoreLabels.md).

## Usage

``` r
DefaultIgnoreLabelDescriptions()
```

## Value

A character vector of label descriptions.

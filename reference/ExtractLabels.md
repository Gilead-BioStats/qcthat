# Extract label names from label objects

Extract label names from label objects

## Usage

``` r
ExtractLabels(lLabels)
```

## Arguments

- lLabels:

  (`list`) List column of label objects as returned by GitHub API.

## Value

A list column of character vectors of label names.

# Normalize the label prefix

Normalize the label prefix

## Usage

``` r
NormalizeLabelPrefix(strLabel)
```

## Arguments

- strLabel:

  (`length-1 character`) The name of the label to create.

## Value

The label with the prefix `"qcthat-"`.

# Flatten empty vectors into NULL

Flatten empty vectors into NULL

## Usage

``` r
NullIfEmpty(x)
```

## Arguments

- x:

  An object to potentially flatten.

## Value

`NULL` if `x` has length 0, otherwise `x`.
